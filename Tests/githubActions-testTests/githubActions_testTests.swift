import XCTest
@testable import githubActions_test

final class githubActions_testTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(githubActions_test().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
